import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const RequestCredentialScreen = () => (
  <SafeAreaView>
    <Text>Request Credential Screen</Text>
  </SafeAreaView>
);

export default RequestCredentialScreen;
