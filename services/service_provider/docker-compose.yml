version: '3.7'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: release
    volumes:
      - .:/usr/app
      - /node_dir/node_modules
    container_name: service_provider
    expose:
      - '8083'
    ports:
      - '8083:8083'